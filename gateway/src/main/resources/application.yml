server:
  port: 18080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: demo-service
          uri: ${DEMO_SERVICE_URL:http://localhost:18081}
          predicates:
            - Path=/demo/**
          filters:
            - StripPrefix=1
